class Stat:

    def __init__(self, games, wins, draw, loses, scores):
        self.games = games
        self.wins = wins
        self.draw = draw
        self.loses = loses
        self.scores = scores
